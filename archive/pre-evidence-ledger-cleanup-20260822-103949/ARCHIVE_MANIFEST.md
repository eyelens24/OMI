# Archived during evidence-ledger rebuild

These files are preserved for reference but are not part of the active Windows-native, local-only Doctor Quant runtime.

| Original path | Reason archived |
| --- | --- |
| `test_investigation_graph.py` | Root-level duplicate; maintained tests live under `tests/`. |
| `test_synthetic_scenario_api.py` | Root-level duplicate; maintained tests live under `tests/`. |
| `doctorquant.spec.js` | Root-level browser-test copy; maintained tests live under `e2e/`. |
| `insufficient-evidence.spec.js` | Root-level browser-test copy; maintained tests live under `e2e/`. |
| `Dockerfile`, `compose.yaml`, `render.yaml` | Deferred deployment paths; Doctor Quant is currently Windows-native and loopback-only. |
| `ALGORITHM_DECISIONS.md`, `DATA_CONTRACT.md`, `FLIGHT_RECORDER.md`, `INCIDENT_LABELS.md`, `ML_PIPELINE.md`, `RESEARCH_COMPONENTS.md` | Historical/research notes superseded by the rebuilt README; retained for reference. |

Nothing in this archive is deleted. Restore a file by moving it back to its original project-relative path.
